# SimpleDb
SimpleDB is a code-first, file-based embedded database library for .NET that lets developers define tables as C# classes and manage them through dependency injection. It provides CRUD operations, foreign keys, unique indexes, triggers, caching, lazy or immediate writes, compression, encryption, and versioned storage support.
